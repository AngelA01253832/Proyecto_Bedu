const Pedido = require('../models/Pedido');

const crearPedido = () =>{

};

const obtenerPedido = () =>{

};

const modificarPedido = () =>{

};

const eliminarPedido = () =>{

};

module.exports = {
    crearPedido,
    obtenerPedido,
    modificarPedido,
    eliminarPedido
};